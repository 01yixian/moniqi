{
  "name": "leek-simulator",
  "version": "1.0.0",
  "description": "纯静态股市模拟器，无需构建",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/"
  },
  "dependencies": {},
  "devDependencies": {}
}